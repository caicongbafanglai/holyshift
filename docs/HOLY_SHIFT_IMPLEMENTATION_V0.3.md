# Holy Shift v0.3 实现基线

## 可玩交付范围

本版是第一章完整纵向切片，不伪装为全岛开放世界。玩家能在神圣广场、圣水池、礼拜集合区与私募教堂入口连续移动、调查、战斗，并完成“圣水变生水”的事件闭环。

主线状态：

1. 与牧司学姐确认异常。
2. 调查圣水池。
3. 在广场中实时清除三只“生水泡影”。
4. 向 Pingu 核对供应链备案，发现被移走的“圣”字痕迹。
5. 向林镇阴确认旧案。
6. 在圣水池旁实时击败“已审批水鬼”。
7. 使用 Holy Shift 把“圣”移回圣水。
8. 前往私募教堂入口，见证电梯显示 `191F / SHIFT`。

## 输入

- `W A S D`：移动
- `Shift`：疾跑；每次按下恢复 8 点 SHIFT
- `Space`：跳跃
- `X + W A S D`：沿当前视角三维飞行；俯视时转为滑翔并恢复耐力。不得绑定浏览器保留快捷键。
- 鼠标：观察
- 鼠标左键 / `J`：三段手杖连击
- 鼠标右键 / `Q`：16.5 米大范围高伤 Holy Shift，消耗 40 SHIFT
- 单按 `Ctrl`：闪避
- `E`：调查 / 交谈
- `B`：打开 / 关闭背包，使用食品
- `V`：第一 / 第三人称
- `R`：返回最近安全点
- `Esc`：暂停

## 可追踪验收要求

下列 ID 是 v0.3 的稳定验收键。任何候选提交必须在评审记录中绑定提交哈希、
测试环境、原始日志和责任人；只写“已实现”不算证据。

| ID | 客观门槛 |
|---|---|
| `HS03-FLOW-001` | 八个主线步骤按序可完成；不得通过残留活敌跳关，终态敌人击败与下一进度必须在同一存档事务中持久化。 |
| `HS03-SAVE-001` | 泡影和 Boss 最终击杀后任意时点刷新均可继续；旧的不一致终态须幂等对账，任务奖励事件恰好一次。 |
| `HS03-SAVE-002` | 任一权威槽出现校验有效的未来 schema/内容版本时必须只读阻断，不能用旧备份覆盖；校验和损坏的伪版本不得误判为未来档。 |
| `HS03-INPUT-001` | 键盘 Enter/Space 可原生激活所有按钮；飞行使用 `X + W A S D`，不得依赖浏览器保留快捷键。 |
| `HS03-A11Y-001` | 开场、背包、商店、暂停、致命错误和尺寸警告均有可访问名称、焦点进入/回收和背景隔离；静止任务 live region 1 秒内不得重复突变。 |
| `HS03-UI-001` | 在 `640/700/701/844/900/901/960/1024/1075/1076/1100/1120/1121/1280` 宽度及对应 `360/390/540/720/768` 高度，HUD、任务、操作卡、Boss 条、Toast、准星和视角控件不得相互遮挡关键内容。 |
| `HS03-DIST-001` | 近战垂直容差 `2.75m`、交互 `3.25m`、敌人侦测 `4m`、敌人攻击 `2.75m`、喷泉恢复垂直容差 `3.25m`；Holy Shift 使用三维 `16.5m` 距离。 |
| `HS03-COLL-001` | 飞行解除后必须落在有地面且无实体碰撞的位置；无安全点时立即安全复位。安全复位不得复活已持久化击败的敌人。 |
| `HS03-AI-001` | 敌人与玩家隔着圣水池时必须确定性绕障；Boss 对测试中登记的 `(0,-20) (0,-16) (0,-14) (6,-2) (-6,-2) (4,-8) (-4,-8)` 七个玩家坐标均须在 60 个模拟秒内产生有效攻击判定。探索性压力网格只证明可达性，不冒充该时限门槛。 |
| `HS03-DEPLOY-001` | 构建基址固定 `/holyshift/`；未知路径真实 404；批准的 CI 产物经哈希校验进入 root-owned 版本目录并原子切换，开发工作树不得作为 web root。 |
| `HS03-SEC-001` | HTTP 只跳 HTTPS、最低 TLS 1.2、HSTS 已分阶段验收、Cloudflare 到源站为严格 TLS，源站不可被公网/未知 Host 绕过。 |
| `HS03-PERF-001` | 参考机、浏览器、画质、场景、采样时长和 P95 门槛必须在章程中冻结；未冻结前任何软件渲染微基准都不能形成发布性能 `PASS`。 |
| `HS03-HUMAN-001` | Gate 2 至少 5 名玩家、Gate 3 至少 6 名新玩家以及真人无障碍/信息识别记录按治理制度预注册；自动化或 AI 不能替代。 |

## 代码与资产分层

- `src/art/materials`：共享色板与材质
- `src/art/modeling`：建模原语、曲线、拱券、饰条与性能工具
- `src/art/characters/<character>`：每名角色独立模型与动画
- `src/art/enemies`：异常敌人模型
- `src/art/architecture`：教堂、圣水池、广场与城市天际线
- `src/art/props`：公告牌、长椅、路灯、红肠补给车等可见道具
- `src/gameplay/combat`：场景内实时战斗、判定、受击与敌人 AI
- `src/gameplay/quests`：第一章状态机与剧情数据
- `src/domain/economy.ts`：码货币、购买、背包消耗与临时耐力上限规则
- `src/world`：资产拼装、碰撞、交互与环境更新
- `src/ui`：不遮挡视野的 HUD、对话、食品商店、背包与设置

## Pingu 食品循环

- 每次主线进度推进只结算一次 `91 码`；结算键写入存档事件账本，重复交谈不能刷取。
- 旧版 v4 存档按当前主线位置追溯补发已完成节点的码，并同步写入同一事件账本；已消费余额不会在后续读取时重复补发。
- Pingu 摊位始终可交互，目前只出售食品，不实现物品、贿赂或真实支付。
- 红肠售价 `18 码`，临时增加 `27.8` 耐力上限。
- 忘情牛肉面售价 `27.8 码`，临时增加 `91` 耐力上限。
- 成吉思鸡售价 `200 码`，临时增加 `278` 耐力上限。
- 购买后明确选择“立即使用”或“放入背包”；`B` 打开背包并可使用库存。
- 同时只保留一个食品增益；食用另一食品会替换当前增益。
- 增益期间最大耐力为 `100 + 食品加成`；当前耐力真正归零时立即清除增益，最大耐力恢复为 `100`。
- 金额以一位小数归一，所有商品 ID 都通过固定目录白名单验证。

## 性能预算

- 共享材质与几何；重复窗、铺装、栏杆和灯具使用实例化。
- 战斗敌人按章节状态创建或休眠，不在后台持续执行 AI。
- 默认关闭动态阴影，以烘焙感顶点色、接触阴影片和环境光层次替代。
- 自动画质根据 P95 帧耗时调整渲染比例。
- 动画与 AI 使用有上限的 `delta`，防止后台切回时穿透或瞬杀。
- 广场群众以职业分组顶点色合批；软件渲染器隐藏远景组并简化近景组。

## 碰撞与防卡死门槛

- 玩家胶囊不能生成在实体碰撞体内。
- 低帧率疾跑、闪避和击退必须分步移动，避免穿墙。
- 每个主线交互物必须存在至少一个有地面、无碰撞、可直达的交互环。
- Pingu 摊位营业侧拥有独立交互锚点与安全环，不与 Pingu 剧情交互争抢。
- 凹角采用滑墙移动；摄像机使用射线回缩避免穿建筑。
- 越界、掉落、非有限坐标或实体重叠会自动回到安全点。
- 飞行和滑翔均使用三维子步进、22 米高度上限和独立岛界；不能穿建筑或从高空越过边界。
- 俯视滑翔采用受控下降、墙面滑动和安全落点搜索；滑翔期间停止飞行消耗并恢复耐力。
- 圣水池南侧设置可见入口台阶，池底为独立落脚面；进入水体后持续恢复生命与 SHIFT。
- 自动网格扫描覆盖广场边缘、喷泉环、教堂立面、公告牌、灯柱与所有主线目标。
